<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
	    <item name="windowActionBar">false</item>
	    <item name="windowNoTitle">true</item>
	    <item name="colorPrimary">@color/dark_gray</item>
    </style>
	<style name="Toolbar" parent="ThemeOverlay.AppCompat.ActionBar">
		<item name="android:textColorPrimary">@drawable/login_button</item>
		<item name="android:actionMenuTextColor">@drawable/login_button</item>
		<item name="actionMenuTextColor">@drawable/login_button</item>
	</style>

</resources>
